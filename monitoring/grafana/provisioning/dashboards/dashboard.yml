apiVersion: 1

providers:
  - name: default
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10   # Recarga el JSON si cambia en disco
    allowUiUpdates: true        # Permite editar desde la UI y guardar
    options:
      path: /var/lib/grafana/dashboards
