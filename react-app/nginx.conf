server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    # Proxy /api/* → py-app service
    location /api/ {
        proxy_pass http://py-app:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
